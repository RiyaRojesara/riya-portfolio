from django.db import models

# No database models needed for a static portfolio.
# All data is managed in views.py context dictionaries.
# Add models here if you want a Django admin-editable portfolio in the future.
